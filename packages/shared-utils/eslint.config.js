import { defineConfig } from 'eslint/config';
import packagesBaseConfig from '../../eslint.packages.base.js';

/**
 * ESLint config for shared-utils package
 */
export default defineConfig([...packagesBaseConfig]);
