# Use the pnpm version pinned in the root package.json "packageManager" field
manage-package-manager-versions=true

# Fail fast if the local Node.js / pnpm version doesn't satisfy "engines"
engine-strict=true

# Hoist a single version of shared peer deps to avoid duplicate-instance bugs
dedupe-peer-dependents=true
