{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "lib": ["esnext"]
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "uploads", "dist", "build"]
}
